spring:
  datasource:
    username: dbadmin
    password: qwerty
    url: jdbc:postgresql://127.0.0.1:5430/toyshopdb
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  show-sql: true